# Changelog

## Unreleased

## 0.1.0 — 2026-08-25

Promoted `0.1.0-rc.4` to stable `0.1.0` (same code, rc suffix dropped) during
the @johnhenry family consolidation. This package was born scoped as part of
the 2026-08 agent-query family rename (`@johnhenry/mcpq`/`a2aq`/`acpq` →
`mcp-query`/`a2a-query`/`acp-query`, with this core extracted underneath
them); it has no prior unscoped npm identity.
